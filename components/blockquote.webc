<blockquote
	webc:root="override"
	:cite="citation"
>
	<slot></slot>
</blockquote>
<p
	webc:if="attribution || citation"
>
	<a
		webc:if="citation"
		:href="citation"
	>
		<cite></cite>
	</a>
	<span
		webc:if="attribution"
		@html="markdown(attribution)"
	></span>
</p>
