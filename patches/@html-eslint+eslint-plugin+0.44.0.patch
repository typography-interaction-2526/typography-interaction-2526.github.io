diff --git a/node_modules/@html-eslint/eslint-plugin/lib/rules/indent/indent.js b/node_modules/@html-eslint/eslint-plugin/lib/rules/indent/indent.js
index f7a35ec..d3968e1 100644
--- a/node_modules/@html-eslint/eslint-plugin/lib/rules/indent/indent.js
+++ b/node_modules/@html-eslint/eslint-plugin/lib/rules/indent/indent.js
@@ -144,6 +144,15 @@ module.exports = {
       if (isTag(node) || isScript(node) || isStyle(node)) {
         return getTagIncreasingLevel(node);
       }
+      if (
+        node.type === NODE_TYPES.Text &&
+        node.parent &&
+        isTag(node.parent) &&
+        indentLevelOptions?.tagChildrenIndent
+      ) {
+        const tagIndent = indentLevelOptions.tagChildrenIndent[node.parent.name.toLowerCase()];
+        if (tagIndent !== undefined) return tagIndent;
+      }
       const type = node.type;
       if (type === NODE_TYPES.Attribute) {
         const optionIndent = indentLevelOptions[type];
